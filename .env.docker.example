# ============================================
# DOCKER ENVIRONMENT VARIABLES - EXAMPLE
# ============================================
# Copy this file to .env.docker and change all passwords!
# Command: cp .env.docker.example .env.docker

# --------------------------------------------
# PostgreSQL Configuration
# --------------------------------------------
POSTGRES_USER=finance_user
POSTGRES_PASSWORD=generate-secure-password-here
POSTGRES_DB=finance_db
POSTGRES_PORT=5432

# --------------------------------------------
# Redis Configuration
# --------------------------------------------
REDIS_PASSWORD=generate-secure-password-here
REDIS_PORT=6379

# --------------------------------------------
# PgAdmin Configuration
# --------------------------------------------
PGADMIN_EMAIL=admin@finance.app
PGADMIN_PASSWORD=generate-admin-password-here
PGADMIN_PORT=5050

# ============================================
# HOW TO GENERATE SECURE PASSWORDS:
# ============================================
# Run this command 3 times to generate passwords:
# openssl rand -base64 32
#
# Then copy each result to:
# 1. POSTGRES_PASSWORD
# 2. REDIS_PASSWORD
# 3. PGADMIN_PASSWORD
