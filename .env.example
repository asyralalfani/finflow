# ============================================
# ENVIRONMENT VARIABLES
# ============================================
# Copy: cp .env.example .env

# --------------------------------------------
# DATABASE (Required)
# --------------------------------------------
# Use password from .env.docker
DATABASE_URL="postgresql://finance_user:your-password-here@localhost:5432/finance_db"

# --------------------------------------------
# REDIS (Optional)
# --------------------------------------------
# Use password from .env.docker
REDIS_URL="redis://:your-password-here@localhost:6379"

# --------------------------------------------
# JWT SECRET (Required)
# --------------------------------------------
# Generate: openssl rand -base64 32
JWT_SECRET="change-this-to-random-32-character-secret"

# --------------------------------------------
# APPLICATION (Required)
# --------------------------------------------
NEXT_PUBLIC_API_URL="http://localhost:3000"
NODE_ENV="development"

# --------------------------------------------
# OPTIONAL
# --------------------------------------------
# PORT=3000
# COOKIE_SECURE="false"
# COOKIE_SAMESITE="lax"

# ============================================
# QUICK SETUP:
# ============================================
# 1. cp .env.example .env
# 2. docker-compose --env-file .env.docker up -d
# 3. Get passwords: cat .env.docker | grep PASSWORD
# 4. Update DATABASE_URL and REDIS_URL above
# 5. Generate JWT: openssl rand -base64 32
# 6. Update JWT_SECRET above
# 7. npm run dev
# ============================================
